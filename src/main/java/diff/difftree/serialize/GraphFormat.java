package diff.difftree.serialize;

/**
 * Format of the graph.
 * A DIFFGRAPH contains more than one root.
 * A DIFFTREE contains exactly one root.
 */
public enum GraphFormat {
    DIFFGRAPH,
    DIFFTREE
}
