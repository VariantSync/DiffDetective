 # if A &&  /* inline
+ comment */ C \
- comment */ B \
  && D //* no continuation
   baz();
 #endif
 
 # if A &&  /* inline
 comment */ C \
  && D //* no continuation
+   baz();
-   vaz();
 #endif
 
 //*ptr=something
 //other comment /* inner
 text
